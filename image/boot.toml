# What goes into the signed boot bundle (`signature || ustar`).
#
# The loader verifies the signature over the whole archive and starts the entries in order.
# `kernel` must be first. The loader refuses a `grants` entry: devices are reached only
# through their handles.
#
# NOTE: not yet consumed by a tool. `./mkimage` currently builds a kernel-only bundle through
# the bench's builder; this file records the intended shape for when `init` and the servers
# exist (docs/INIT.md, docs/PLAN.md).

[[entry]]
name = "kernel"
package = "redoubt-kernel"

# [[entry]]
# name = "init"
# package = "redoubt-init"

# [[entry]]
# name = "keyd"
# package = "redoubt-keyd"

# [[entry]]
# name = "consoled"
# package = "redoubt-consoled"
