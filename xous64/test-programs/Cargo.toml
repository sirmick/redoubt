[package]
name = "test-programs"
version = "0.1.0"
edition = "2021"
license = "MIT OR Apache-2.0"
description = "no_std programs that run inside Xous under the test bench (xous64/testbench)"

[dependencies]
xous = "0.9.70"
uart_16550 = { version = "0.3", default-features = false }
