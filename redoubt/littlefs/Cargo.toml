[package]
name = "littlefs"
version = "0.1.0"
edition = "2021"
license = "MIT OR Apache-2.0"
description = "littlefs (on-disk version 2.1) in pure Rust, no_std + alloc, for fsd"

# No dependencies: this crate parses an untrusted medium (TENETS.md 5).
[dependencies]
