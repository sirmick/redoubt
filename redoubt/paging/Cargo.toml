[package]
name = "paging"
version = "0.1.0"
edition = "2021"
license = "MIT OR Apache-2.0"
description = "Typed RISC-V page tables (Sv32 + Sv39): the one place page-table memory is touched"

[dependencies]
bitflags = "2"
