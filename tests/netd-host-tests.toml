description = "netd against a hostile virtio-net device: bring-up refusals, ring lies refused and the device reset, bad frames from the wire dropped (never a lie), transmits carrying exactly header and frame, busy then broken, one client and no labelled callers, and 20,000 randomized hostile devices and requests (answer 174)"
kind = "host-tests"
# QEMU's honest virtio-net cannot lie; the malicious rings, ids, lengths, headers and scribbles
# live in servers/netd/tests/, against redoubt_netd::fake::FakeNic. These host tests do not
# establish boot integration: the tests/net rig does that.
packages = ["redoubt-netd"]
