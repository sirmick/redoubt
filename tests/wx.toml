description = "W^X: trusted checker observes native child exit notices for attempted data execution and code writes"
arch = ["rv64", "rv32"]
kind = "boot"
debug_assertions = true
programs = ["wx-test"]
expect = [
    '^\[wx\] checker: kernel refuses W\+X and empty permissions$',
    '^\[wx\] checker: attack 1 faulted with kernel cause 12$',
    '^\[wx\] checker: attack 2 faulted with kernel cause 15$',
]
forbid = ['\[wx\] FAIL']
poweroff = true
