description = "ipd's vendored TCP/IP stack (smoltcp with alloc, medium-ethernet, proto-ipv4, socket-tcp, and its dependencies) builds no_std for both widths"
arch = ["rv64", "rv32"]
kind = "build"
package = "redoubt-vendor-check"
