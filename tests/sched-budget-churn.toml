description = "Budget churn gains nothing (WP-K5, pass inheritance): blocking, spinning-parent, deadline-timed and fresh-intermediate churn leave an equal-weight victim half; a shell that keeps giving a budget half its weight and taking it back is neither starved nor favoured (virtual time, checked build)"
arch = ["rv64", "rv32"]
kind = "boot"
debug_assertions = true
icount = "shift=3,sleep=off"
programs = ["sched-budget-churn"]
timeout_secs = 600
expect = [
    '^\[budget-churn\] ok: blocking churner',
    '^\[budget-churn\] ok: spinning parent',
    '^\[budget-churn\] ok: deadline just after',
    '^\[budget-churn\] ok: fresh intermediates',
    '^\[budget-churn\] ok: shell giving and taking back half its weight',
    '^SCHED-BUDGET-CHURN TEST PASSED$',
]
forbid = ['\[budget-churn\] FAIL']
poweroff = true
