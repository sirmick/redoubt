description = "The vendored crates (vendor/README.md, answer 174) are byte for byte what crates.io published, nothing added or removed, and Cargo.lock builds them from vendor/, with no registry copy; cfg-if and bitflags, left to the lockfile, are pinned by version and checksum"
kind = "host-tests"
packages = ["redoubt-vendor-check"]
