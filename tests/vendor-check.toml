description = "The vendored crates (vendor/README.md) are byte for byte what vendor/SHA256SUMS records (integrity; provenance is tools/vendor-check/provenance.sh, run in review), nothing added or removed, and Cargo.lock and cargo metadata build them from vendor/, with no registry copy; cfg-if and bitflags, left to the lockfile, are pinned by version and checksum"
kind = "host-tests"
packages = ["redoubt-vendor-check"]
