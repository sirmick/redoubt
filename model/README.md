# The executable model

A host-only model of the kernel and the steward's policy, checked by property tests and
mutations. What it models and how to run it is in [the executable model](../docs/kernel/model.md).
